spring:
  application:
    name: company-person-task-api
  neo4j:
    uri: ${NEO4J_URI:bolt://localhost:7687}
    authentication:
      username: ${NEO4J_USERNAME:neo4j}
      password: ${NEO4J_PASSWORD:qwerty123}

server:
  port: ${SERVER_PORT:8080}

logging:
  level:
    org.springframework.data.neo4j: INFO
